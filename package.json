{
  "name": "spotify-auto-paging",
  "version": "0.0.0",
  "description": "Auto paginate spotify endpoints.",
  "main": "dist/",
  "scripts": {
    "start": "nodemon -e ts -w ./src -x npm run start:ts",
    "start:ts": "ts-node src/example.ts",
    "prepare": "tsc",
    "prepublishOnly": "npm run prepare",
    "test": "echo \"Error: no test specified\" && exit 0"
  },
  "keywords": [],
  "author": "Akram Saouri",
  "license": "ISC",
  "dependencies": {
    "isomorphic-fetch": "^2.2.1"
  },
  "devDependencies": {
    "@babel/preset-flow": "^7.9.0",
    "@types/isomorphic-fetch": "0.0.35",
    "@types/node": "^13.13.2",
    "ts-node": "^8.9.0",
    "typescript": "^3.8.3"
  }
}
